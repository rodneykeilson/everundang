apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: everundang
  labels:
    app.kubernetes.io/name: everundang
    app.kubernetes.io/component: frontend
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: everundang
    app.kubernetes.io/component: frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
